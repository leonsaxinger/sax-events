PARTNER-LOGOS
=============

Vorhanden: citybeats.svg, empire.png, drip.png
FEHLT noch: silberpfeil.png

Bitte ablegen:
  silberpfeil.png   -> TRANSPARENTES PNG (oder SVG), Logo in WEISS auf transparentem
                       Hintergrund. Die mitgelieferte Datei hatte einen hellen
                       Hintergrund-Kasten und ist daher unbrauchbar -> aktuell wird
                       stattdessen der Schriftzug "SILBERPFEIL" angezeigt.
                       Sobald die saubere Datei hier liegt, erscheint sie automatisch.

Darstellung (in data/site.ts unter "partners"):
  mode "white"   -> Logo wird zu reinem Weiss gefaerbt (citybeats, drip, silberpfeil)
  mode "natural" -> Logo unveraendert (empire: schwarze Bloecke + weisser Text)

Weitere Partner: Datei hier ablegen + in data/site.ts ergaenzen.
